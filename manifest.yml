applications:
    - name: frontend-exercise
      command: npm start
      buildpack: nodejs_buildpack
      memory: 64M
      disk_quota: 256M
      instances: 1
      env:
          NODE_ENV: production
